#kaocha/v1
{:tests [{:id :peer
          :test-paths ["test/peer"]}
         {:id :client
          :test-paths ["test/client"]}]
 ;;:kaocha/fail-fast? true
 ;;:watch? true
 ;; :reporter kaocha.report.progress/progress
 :plugins [:kaocha.plugin/notifier]
 }
