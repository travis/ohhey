export const iBelieve = (history, {slug}) => history.push(`/ibelieve/${slug}`)
export const iDontBelieve = (history, {slug}) => history.push(`/idontbelieve/${slug}`)
export const someSay = (history, {slug}) => history.push(`/somesay/${slug}`)
